# Pipeline: Bump Versions (non-Rush) -- AzDO definition 10 in the GitHubProjects project
# Trigger: Automatically when changes merge to main that include change files or pipeline YAML.
# Purpose: Bump versions for the "noRush" version policy, build, pack, create/update GitHub PR.
#          The packed tarballs are published by the "esrp-publish-rushstack.yaml" pipeline once the
#          resulting version bump PR is merged.

trigger:
  batch: true
  branches:
    include:
      - main
  paths:
    include:
      - 'common/changes/*'
      - 'common/config/rush/version-policies.json'
      - 'common/config/azure-pipelines/**'

pr: none

variables:
  - name: FORCE_COLOR
    value: 1
  - name: BaseBranch
    value: $[ replace(variables['Build.SourceBranch'], 'refs/heads/', '') ]

resources:
  repositories:
    - repository: 1esPipelines
      type: git
      name: 1ESPipelineTemplates/1ESPipelineTemplates
      ref: refs/tags/release
    - repository: rushstackGitHubApp
      type: github
      name: microsoft/rushstack
      endpoint: GitHubProjects
      ref: refs/heads/main

extends:
  template: v1/1ES.Official.PipelineTemplate.yml@1esPipelines
  parameters:
    settings:
      networkIsolationPolicy: CFSClean,CFSClean2,CFSClean3,DefaultDeny
    sdl:
      sourceRepositoriesToScan:
        include:
          - repository: rushstackGitHubApp
    pool:
      name: Azure-Pipelines-1ESPT-ExDShared
      os: windows
    stages:
      - template: /common/config/azure-pipelines/templates/bump-versions-stages.yaml@self
        parameters:
          VersionPolicyName: noRush
          StageName: BumpRushstackVersions
          StageDisplayName: 'Bump Rushstack Versions and Create PR'
          CheckoutRepository: rushstackGitHubApp
          FeatureBranch: 'automated/bump-versions-rushstack'
          PrTitle: 'Bump Rushstack package versions'
          PrDescription: |
            Automated version bump generated by Azure DevOps Pipeline.

            This PR contains version bumps for the "noRush" version policy.

            **Pipeline Run:** $(System.CollectionUri)$(System.TeamProject)/_build/results?buildId=$(Build.BuildId)
