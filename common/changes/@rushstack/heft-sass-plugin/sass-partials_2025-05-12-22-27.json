{
  "changes": [
    {
      "packageName": "@rushstack/heft-sass-plugin",
      "comment": "Fix an issue where the SCSS module classifier evaluated SCSS partials instead of ignoring them (since they aren't directly importable).",
      "type": "patch"
    }
  ],
  "packageName": "@rushstack/heft-sass-plugin"
}