{
  "changes": [
    {
      "packageName": "@microsoft/gulp-core-build",
      "type": "patch",
      "comment": "Adding 'del' as a dependency, which should fix gulp clean."
    },
    {
      "packageName": "@microsoft/node-library-build",
      "type": "patch",
      "comment": "Adding 'del' as a dependency, which should fix gulp clean."
    },
    {
      "packageName": "@microsoft/web-library-build",
      "type": "patch",
      "comment": "Adding 'del' as a dependency, which should fix gulp clean."
    }
  ]
}
