{
  "name": "heft-copy-files-test",
  "description": "Building this project tests copying files with Heft",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "heft build --clean",
    "_phase:lite-build": "heft run --only build -- --clean"
  },
  "devDependencies": {
    "@rushstack/heft": "workspace:*"
  }
}
