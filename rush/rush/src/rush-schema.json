{
  "title": "Rush Configuration",
  "description": "Configuration file for the Rush multi-package build tool",

  "type": "object",
  "properties": {
    "$schema": {
      "description": "The metadata to indicate the JSON schema, required by some editors such as VS2015",
      "type": "string"
    },
    "commonFolder": {
      "description": "Specifies the name of a top-level global folder containing a package.json file with a superset of all dependencies for all projects in the repository.  Example: 'common'",
      "type": "string"
    },
    "projects": {
      "description": "A mapping of NPM package names to their build information",
      "type": "object",
      "patternProperties": {
        "^[a-zA-Z0-9_@/-]+$": {
          "type": "object",
          "properties": {
            "path": {
              "description": "The path to the project folder relative to the repository root",
              "type": "string"
            },
            "dependencies": {
              "description":  "An array of the NPM package names in this repository that this project directly depends on",
              "type": "array",
              "items": {
                "type": "string"
              },
              "minItems": 0,
              "uniqueItems": true
            }
          }
        }
      }
    }
  },
  "additionalProperties": false,

  "required": [ "commonFolder", "projects" ]
}