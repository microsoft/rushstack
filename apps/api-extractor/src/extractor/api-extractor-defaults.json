{
  // ("compiler" is required)

  "project": {
    // ("entryPointSourceFile" is required)

    "externalJsonFileFolders": [ ]
  },

  "policies": {
    "namespaceSupport": "conservative"
  },

  "apiReviewFile": {
    "enabled": true,
    "apiReviewFolder": "./etc",
    "tempFolder": "./temp"
  },

  "apiJsonFile": {
    "enabled": true,
    "outputFolder": "./dist"
  },

  "packageTypings": {
    "enabled": false,
    "outputFolder": "./dist",
    "internalFilename": "index-internal.d.ts",
    "previewFilename": "index-preview.d.ts",
    "publicFilename": "index-public.d.ts"
  }
}
