{
  "extends": "./node_modules/@rushstack/heft-node-rig/profiles/default/tsconfig-base.json",

  "compilerOptions": {
    // Needed because JSZip is missing a typing, and attempting to re-add the typing conflicts with
    // the typescript library
    // https://github.com/Stuk/jszip/issues/693
    "skipLibCheck": true
  }
}
