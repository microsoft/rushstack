{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Pnpm configuration",
  "description": "The configuration file for pnpm.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "description": "Part of the JSON Schema standard, this optional keyword declares the URL of the schema that the file conforms to. Editors may download the schema and use it to perform syntax highlighting.",
      "type": "string"
    },
    "pnpmFieldInRootPackageJson": {
      "description": "This field will be patched as the 'pnpm' field in the root package.json of pnpm workspace created by Rush.js",
      "type": "object"
    }
  }
}
